---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clickpipes.clickpipe.clickhouse.crossplane.io
spec:
  group: clickpipe.clickhouse.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - clickhouse
    kind: Clickpipe
    listKind: ClickpipeList
    plural: clickpipes
    singular: clickpipe
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Clickpipe is the Schema for the Clickpipes API. You can use
          the clickhouse_clickpipe resource to create and manage ClickPipes data ingestion
          pipelines in ClickHouse Cloud. Supported source types: Kafka (Confluent,
          MSK, Azure Event Hubs, Redpanda, WarpStream), Object Storage (S3, GCS, Azure
          Blob), Kinesis, Postgres CDC, MySQL CDC, BigQuery, and MongoDB CDC. Known
          limitations: ClickPipe does not support table updates for managed tables.
          If you need to update the table schema, you will have to do that externally.Changing
          the source type of an existing ClickPipe will force replacement (destroy
          and recreate).'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClickpipeSpec defines the desired state of Clickpipe
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  destination:
                    description: The destination for the ClickPipe. (see
                      below for nested schema)
                    properties:
                      columns:
                        description: The list of columns for the
                          ClickHouse table. Required for all sources except Postgres
                          CDC (where columns are determined from source tables). (see
                          below for nested schema)
                        items:
                          properties:
                            name:
                              description: |-
                                The name of the column.
                              type: string
                            type:
                              description: |-
                                The type of the column.
                              type: string
                          type: object
                        type: array
                      database:
                        description: |-
                          The name of the ClickHouse database. Default is `default`.
                        type: string
                      managedTable:
                        description: |-
                          Whether the table is managed by ClickHouse Cloud. If `false`, the table must exist in the database. Default is `true`. **Not applicable to database/CDC pipes** (Postgres, MySQL, BigQuery, MongoDB): for those sources destination tables are always managed per-table via `table_mappings`, so this field is ignored and not sent to the API.
                        type: boolean
                      roles:
                        description: |-
                          ClickPipe will create a ClickHouse user with these roles. Add your custom roles here if required.
                        items:
                          type: string
                        type: array
                      table:
                        description: |-
                          The name of the ClickHouse table. Required for all sources except Postgres CDC (where tables are created from table_mappings).
                        type: string
                      tableDefinition:
                        description: table via table_mappings (each mapping's target_table,
                          table_engine, sorting_keys, etc.), so configuring this is
                          rejected at plan time. (see below for nested schema)
                        properties:
                          engine:
                            description: The engine of the ClickHouse
                              table. (see below for nested schema)
                            properties:
                              columnIds:
                                description: |-
                                  Column IDs to sum for SummingMergeTree engine. Required when engine type is `SummingMergeTree`.
                                items:
                                  type: string
                                type: array
                              type:
                                description: |-
                                  The type of the engine. Supported engines: `MergeTree`, `ReplacingMergeTree`, `SummingMergeTree`, `Null`.
                                type: string
                              versionColumnId:
                                description: |-
                                  Column ID to use as version for ReplacingMergeTree engine. Required when engine type is `ReplacingMergeTree`.
                                type: string
                            type: object
                          partitionBy:
                            description: |-
                              The column to partition the table by.
                            type: string
                          primaryKey:
                            description: |-
                              The primary key of the table.
                            type: string
                          sortingKey:
                            description: |-
                              The list of columns for the sorting key.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  fieldMappings:
                    description: Field mapping between source and
                      destination table. (see below for nested schema)
                    items:
                      properties:
                        destinationField:
                          description: |-
                            The name of the column in destination table.
                          type: string
                        sourceField:
                          description: |-
                            The name of the source field.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      The name of the ClickPipe.
                    type: string
                  scaling:
                    description: (see below for nested schema)
                    properties:
                      replicaCpuMillicores:
                        description: |-
                          The CPU allocation per replica in millicores. Must be between 125 and 2000.
                        type: number
                      replicaMemoryGb:
                        description: |-
                          The memory allocation per replica in GB. Must be between 0.5 and 8.0.
                        type: number
                      replicas:
                        description: |-
                          The number of desired replicas for the ClickPipe. Default is 1. The maximum value is 10.
                        type: number
                    type: object
                  serviceId:
                    description: |-
                      The ID of the service to which the ClickPipe belongs.
                    type: string
                  serviceIdRef:
                    description: Reference to a Service in clickhouse to populate
                      serviceId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceIdSelector:
                    description: Selector for a Service in clickhouse to populate
                      serviceId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  settings:
                    description: |-
                      Advanced configuration options for the ClickPipe. These settings are specific to each pipe. For the complete list of available options, see the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger (search for the ClickPipes settings endpoint).
                    x-kubernetes-preserve-unknown-fields: true
                  source:
                    description: The data source for the ClickPipe. At
                      least one source configuration must be provided. (see below
                      for nested schema)
                    properties:
                      bigquery:
                        description: The BigQuery source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Google Cloud service account JSON key file content, base64 encoded.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel workers during initial load.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the BigQuery pipe. (`snapshot`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                            type: object
                          snapshotStagingPath:
                            description: |-
                              GCS bucket path for staging snapshot data (e.g., gs://my-bucket/staging/). Data will be automatically cleaned up after initial load.
                            type: string
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceDatasetName:
                                  description: |-
                                    Source BigQuery dataset name.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in BigQuery.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                        type: object
                      kafka:
                        description: The Kafka source configuration for
                          the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kafka source. (`PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`, `IAM_ROLE`, `IAM_USER`, `MUTUAL_TLS`). Default is `PLAIN`.
                            type: string
                          brokers:
                            description: |-
                              The list of Kafka bootstrap brokers. (comma separated)
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificates to validate the broker's certificate.
                            type: string
                          consumerGroup:
                            description: |-
                              <ID> will be used.
                              Consumer group of the Kafka source. If not provided `clickpipes-<ID>` will be used.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the Kafka source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              certificateSecretRef:
                                description: |-
                                  PEM encoded client certificate for mTLS authentication. Use with `MUTUAL_TLS` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              connectionStringSecretRef:
                                description: |-
                                  The connection string for the Kafka source. Use with `azureeventhub` Kafka source type. Use with `PLAIN` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordSecretRef:
                                description: |-
                                  The password for the Kafka source. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the Kafka source. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              privateKeySecretRef:
                                description: |-
                                  PEM encoded client private key for mTLS authentication. Use with `MUTUAL_TLS` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the Kafka source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              usernameSecretRef:
                                description: |-
                                  The username for the Kafka source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          exactlyOnce:
                            description: |-
                              Enable exactly-once delivery. Guarantees every Kafka record is inserted exactly once across restarts and rebalances.
                            type: boolean
                          format:
                            description: |-
                              The format of the Kafka source. (`JSONEachRow`, `Avro`, `AvroConfluent`, `Protobuf`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kafka source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kafka#iam
                            type: string
                          offset:
                            description: The Kafka offset. (see below
                              for nested schema)
                            properties:
                              strategy:
                                description: |-
                                  The offset strategy for the Kafka source. (`from_beginning`, `from_latest`, `from_timestamp`)
                                type: string
                              timestamp:
                                description: |-
                                  01-01T00:00)
                                  The timestamp for the Kafka offset. Use with `from_timestamp` offset strategy. (format `2021-01-01T00:00`)
                                type: string
                            type: object
                          reversePrivateEndpointIds:
                            description: |-
                              The list of reverse private endpoint IDs for the Kafka source. (comma separated)
                            items:
                              type: string
                            type: array
                          schemaRegistry:
                            description: The schema registry for the
                              Kafka source. (see below for nested schema)
                            properties:
                              authentication:
                                description: |-
                                  SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                                  The authentication method for the Schema Registry. Only supported is `PLAIN`.
                                type: string
                              credentials:
                                description: The credentials
                                  for BigQuery access. (see below for nested schema)
                                properties:
                                  passwordSecretRef:
                                    description: |-
                                      The password for the Schema Registry. Either `password` or `password_wo` must be provided.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                  passwordWoSecretRef:
                                    description: |-
                                      Write-only password for the Schema Registry. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                  passwordWoVersion:
                                    description: |-
                                      Version trigger for `password_wo`. Increment to push a new password to the API.
                                    type: number
                                  usernameSecretRef:
                                    description: |-
                                      The username for the Schema Registry.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                type: object
                              url:
                                description: |-
                                  The URL of the schema registry.
                                type: string
                            type: object
                          topics:
                            description: |-
                              The list of Kafka topics. (comma separated)
                            type: string
                          type:
                            description: |-
                              The type of the Kafka source. (`kafka`, `redpanda`, `confluent`, `msk`, `warpstream`, `azureeventhub`, `gcmk`). Default is `kafka`.
                            type: string
                        type: object
                      kinesis:
                        description: The Kinesis source configuration
                          for the ClickPipe. Only authentication, iam_role and access_key
                          can be updated in place; changing any other field forces
                          resource replacement (destroy and recreate). (see below
                          for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kinesis source. (`IAM_ROLE`, `IAM_USER`).
                            type: string
                          format:
                            description: |-
                              The format of the Kinesis source. (`JSONEachRow`, `Avro`, `AvroConfluent`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kinesis source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kinesis.
                            type: string
                          iteratorType:
                            description: |-
                              The iterator type for the Kinesis source. (`TRIM_HORIZON`, `LATEST`, `AT_TIMESTAMP`)
                            type: string
                          region:
                            description: |-
                              The AWS region of the Kinesis stream.
                            type: string
                          streamName:
                            description: |-
                              The name of the Kinesis stream.
                            type: string
                          timestamp:
                            description: |-
                              01-01T00:00)
                              The timestamp for the Kinesis source. Use with `AT_TIMESTAMP` iterator type. (format `2021-01-01T00:00`)
                            type: string
                          useEnhancedFanOut:
                            description: |-
                              Whether to use enhanced fan-out consumer.
                            type: boolean
                        type: object
                      mongodb:
                        description: The MongoDB CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MongoDB server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the MongoDB instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the MongoDB instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              username:
                                description: |-
                                  The username for the MongoDB instance.
                                type: string
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MongoDB connection. Defaults to false (TLS enabled).
                            type: boolean
                          readPreference:
                            description: |-
                              MongoDB read preference for replica set reads. (`primary`, `primaryPreferred`, `secondary`, `secondaryPreferred`, `nearest`)
                            type: string
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MongoDB DELETE operations.
                                type: boolean
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch during CDC replication.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the MongoDB pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows per partition during the snapshot phase.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of collections to snapshot in parallel during the initial load phase.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MongoDB during CDC replication.
                                type: number
                              useJsonNativeFormat:
                                description: |-
                                  Store JSON values in native ClickHouse JSON format. When disabled, JSON data is stored as String.
                                type: boolean
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                sourceCollection:
                                  description: |-
                                    MongoDB source collection name.
                                  type: string
                                sourceDatabaseName:
                                  description: |-
                                    MongoDB source database name.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    ClickHouse target table name. The table will be created automatically if it does not exist.
                                  type: string
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections.
                            type: string
                          uri:
                            description: |-
                              MongoDB connection URI. Supports both standard URIs (mongodb://...) and SRV URIs (mongodb+srv://...).
                            type: string
                        type: object
                      mysql:
                        description: The MySQL CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for MySQL connection. Supported values: `basic`, `IAM_ROLE`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MySQL server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the MySQL instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the MySQL instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              usernameSecretRef:
                                description: |-
                                  The username for the MySQL instance.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MySQL connection.
                            type: boolean
                          host:
                            description: |-
                              The hostname of the MySQL instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `IAM_ROLE`.
                            type: string
                          port:
                            description: |-
                              The port of the MySQL instance. Default is 3306.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MySQL DELETE operations.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMechanism:
                                description: |-
                                  Replication mechanism for the MySQL pipe. (`GTID`, `FILE_POS`). Default is `GTID`. Mechanisms other than `GTID` (e.g. `FILE_POS`) must be enabled for your organization; contact ClickHouse support to enable this feature.
                                type: string
                              replicationMode:
                                description: |-
                                  Replication mode for the MySQL pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MySQL.
                                type: number
                              useCompression:
                                description: |-
                                  Enable compression for the MySQL replication connection.
                                type: boolean
                            type: object
                          skipCertVerification:
                            description: |-
                              Skip certificate verification for the MySQL connection.
                            type: boolean
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Must be an indexed column of integer, date, datetime, or timestamp type.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema (database) name in MySQL.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in MySQL.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of MySQL-compatible source. (`mysql`, `rdsmysql`, `auroramysql`, `planetscalevitess`, `mariadb`, `rdsmariadb`). Default is `mysql`.
                            type: string
                        type: object
                      objectStorage:
                        description: The compatible object storage source
                          configuration for the ClickPipe. (see below for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              CONNECTION_STRING is for Azure Blob Storage. IAM_ROLE and IAM_USER are for AWS S3. IAM_USER and SERVICE_ACCOUNT are for GCS. If not provided, no authentication is used
                            type: string
                          azureContainerName:
                            description: |-
                              Container name for Azure Blob Storage. Required when type is azureblobstorage. Example: `mycontainer`
                            type: string
                          compression:
                            description: |-
                              Compression algorithm used for the files.. (`none`, `auto`, `gzip`, `brotli`, `br`, `xz`, `LZMA`, `zstd`)
                            type: string
                          connectionStringSecretRef:
                            description: |-
                              Connection string for Azure Blob Storage authentication. Required when authentication is CONNECTION_STRING. Example: `DefaultEndpointsProtocol=https;AccountName=myaccount;AccountKey=mykey;EndpointSuffix=core.windows.net`
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          delimiter:
                            description: |-
                              The delimiter for the S3 source. Default is `,`.
                            type: string
                          format:
                            description: |-
                              The format of the S3 objects. (`JSONEachRow`, `CSV`, `CSVWithNames`, `Parquet`, `Avro`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the S3 source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#authentication
                            type: string
                          isContinuous:
                            description: |-
                              If set to true, the pipe will continuously read new files from the source. If set to false, the pipe will read the files only once. New files have to be uploaded lexically order.
                            type: boolean
                          path:
                            description: |-
                              Path to the file(s) within the Azure container. Used for Azure Blob Storage sources. You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations. Example: `data/logs/*.json`
                            type: string
                          queueUrl:
                            description: |-
                              Queue URL for event-based continuous ingestion. When provided, files are ingested based on event notifications rather than lexicographical order. Only applicable when `is_continuous` is `true` and authentication is provided. For S3: SQS URL in the format `https://sqs.{region}.amazonaws.com/{account-id}/{queue-name}`. For GCS: Pub/Sub subscription in the format `projects/{project}/subscriptions/{subscription}`.
                            type: string
                          serviceAccountKeySecretRef:
                            description: |-
                              Base64-encoded GCP service account JSON key for GCS authentication. Required when authentication is `SERVICE_ACCOUNT`.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          skipInitialLoad:
                            description: |-
                              If set to true, skips the initial load and only ingests files delivered by queue notifications. Only applicable when `queue_url` is provided.
                            type: boolean
                          startAfter:
                            description: |-
                              06-01/). Cannot be provided when skip_initial_load is true.
                              Skip all files up to and including this object key during the initial load (e.g. `events/2026-06-01/`). Cannot be provided when `skip_initial_load` is true.
                            type: string
                          type:
                            description: |-
                              The type of the S3-compatible source (`s3`, `gcs`, `azureblobstorage`). Default is `s3`.
                            type: string
                          url:
                            description: |-
                              The URL of the S3/GCS bucket. Required for S3 and GCS types. Not used for Azure Blob Storage (use path and azure_container_name instead). You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations
                            type: string
                        type: object
                      postgres:
                        description: The Postgres CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for Postgres connection. Supported values: `basic`, `iam_role`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the Postgres server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the Postgres instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the Postgres instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              usernameSecretRef:
                                description: |-
                                  The username for the Postgres instance.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          database:
                            description: |-
                              The database name of the Postgres instance.
                            type: string
                          host:
                            description: |-
                              The hostname of the Postgres instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `iam_role`.
                            type: string
                          port:
                            description: |-
                              The port of the Postgres instance. Default is 5432.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for PostgreSQL DELETE operations.
                                type: boolean
                              enableFailoverSlots:
                                description: |-
                                  Enable failover for created replication slot. Requires a replication slot to NOT be set.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              publicationName:
                                description: |-
                                  Publication name to use for replication. If not provided, ClickPipes will create one.
                                type: string
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the Postgres pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              replicationSlotName:
                                description: |-
                                  Replication slot name to use for replication. Only applicable when replication_mode is `cdc_only`.
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from Postgres.
                                type: number
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Only beneficial for PostgreSQL 13 (no benefit for PG14+, which supports indexed ctid scans). Must be an indexed column of type: `smallint`, `integer`, `bigint`, `timestamp without time zone`, or `timestamp with time zone`. Unrelated to ClickHouse partitioning.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema name in Postgres.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in Postgres.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of the Postgres source. (`postgres`, `supabase`, `neon`, `alloydb`, `planetscale`, `rdspostgres`, `aurorapostgres`, `cloudsqlpostgres`, `azurepostgres`, `crunchybridge`, `tigerdata`). Default is `postgres`.
                            type: string
                        type: object
                      pubsub:
                        description: The GCP Pub/Sub source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          ackDeadline:
                            description: |-
                              Acknowledgement deadline in seconds (10–600).
                            type: number
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Pub/Sub source. Currently only `SERVICE_ACCOUNT` is supported.
                            type: string
                          enableOrdering:
                            description: |-
                              Whether to enable ordered message delivery. Immutable — changing it requires destroy+create because ordered delivery is a property of the subscription at creation time.
                            type: boolean
                          filter:
                            description: |-
                              Optional Pub/Sub subscription filter expression (CEL). Max 256 characters. Immutable — changing it requires destroy+create because the underlying subscription filter cannot be edited in place.
                            type: string
                          format:
                            description: |-
                              The message format of the Pub/Sub topic. (`JSONEachRow`, `Avro`, `Protobuf`)
                            type: string
                          projectId:
                            description: |-
                              The GCP project ID that owns the Pub/Sub topic.
                            type: string
                          seekTimestamp:
                            description: |-
                              04-10T12:00:00Z). Required when seek_type = "timestamp"; must be omitted otherwise.
                              RFC 3339 timestamp (e.g. `2026-04-10T12:00:00Z`). Required when `seek_type = "timestamp"`; must be omitted otherwise.
                            type: string
                          seekType:
                            description: |-
                              The starting position for consuming the subscription. (`latest`, `earliest`, `timestamp`)
                            type: string
                          serviceAccountKey:
                            description: encoded GCP service account JSON key for
                              GCS authentication. Required when authentication is
                              SERVICE_ACCOUNT.
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Base64-encoded GCP service account JSON key file contents.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          topic:
                            description: |-
                              The Pub/Sub topic name (not the fully-qualified path).
                            type: string
                        type: object
                    type: object
                  stopped:
                    description: |-
                      Whether the ClickPipe should be stopped. Default is `false` (ClickPipe will be running). Cannot be set to `true` on creation — the ClickPipe must be created in a running state and then stopped via a subsequent apply.
                    type: boolean
                  triggerResync:
                    description: |-
                      Set to `true` to trigger a resync operation. Only applicable for Postgres pipes. Automatically resets to `false` after the resync is triggered.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  destination:
                    description: The destination for the ClickPipe. (see
                      below for nested schema)
                    properties:
                      columns:
                        description: The list of columns for the
                          ClickHouse table. Required for all sources except Postgres
                          CDC (where columns are determined from source tables). (see
                          below for nested schema)
                        items:
                          properties:
                            name:
                              description: |-
                                The name of the column.
                              type: string
                            type:
                              description: |-
                                The type of the column.
                              type: string
                          type: object
                        type: array
                      database:
                        description: |-
                          The name of the ClickHouse database. Default is `default`.
                        type: string
                      managedTable:
                        description: |-
                          Whether the table is managed by ClickHouse Cloud. If `false`, the table must exist in the database. Default is `true`. **Not applicable to database/CDC pipes** (Postgres, MySQL, BigQuery, MongoDB): for those sources destination tables are always managed per-table via `table_mappings`, so this field is ignored and not sent to the API.
                        type: boolean
                      roles:
                        description: |-
                          ClickPipe will create a ClickHouse user with these roles. Add your custom roles here if required.
                        items:
                          type: string
                        type: array
                      table:
                        description: |-
                          The name of the ClickHouse table. Required for all sources except Postgres CDC (where tables are created from table_mappings).
                        type: string
                      tableDefinition:
                        description: table via table_mappings (each mapping's target_table,
                          table_engine, sorting_keys, etc.), so configuring this is
                          rejected at plan time. (see below for nested schema)
                        properties:
                          engine:
                            description: The engine of the ClickHouse
                              table. (see below for nested schema)
                            properties:
                              columnIds:
                                description: |-
                                  Column IDs to sum for SummingMergeTree engine. Required when engine type is `SummingMergeTree`.
                                items:
                                  type: string
                                type: array
                              type:
                                description: |-
                                  The type of the engine. Supported engines: `MergeTree`, `ReplacingMergeTree`, `SummingMergeTree`, `Null`.
                                type: string
                              versionColumnId:
                                description: |-
                                  Column ID to use as version for ReplacingMergeTree engine. Required when engine type is `ReplacingMergeTree`.
                                type: string
                            type: object
                          partitionBy:
                            description: |-
                              The column to partition the table by.
                            type: string
                          primaryKey:
                            description: |-
                              The primary key of the table.
                            type: string
                          sortingKey:
                            description: |-
                              The list of columns for the sorting key.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  fieldMappings:
                    description: Field mapping between source and
                      destination table. (see below for nested schema)
                    items:
                      properties:
                        destinationField:
                          description: |-
                            The name of the column in destination table.
                          type: string
                        sourceField:
                          description: |-
                            The name of the source field.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      The name of the ClickPipe.
                    type: string
                  scaling:
                    description: (see below for nested schema)
                    properties:
                      replicaCpuMillicores:
                        description: |-
                          The CPU allocation per replica in millicores. Must be between 125 and 2000.
                        type: number
                      replicaMemoryGb:
                        description: |-
                          The memory allocation per replica in GB. Must be between 0.5 and 8.0.
                        type: number
                      replicas:
                        description: |-
                          The number of desired replicas for the ClickPipe. Default is 1. The maximum value is 10.
                        type: number
                    type: object
                  serviceId:
                    description: |-
                      The ID of the service to which the ClickPipe belongs.
                    type: string
                  serviceIdRef:
                    description: Reference to a Service in clickhouse to populate
                      serviceId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceIdSelector:
                    description: Selector for a Service in clickhouse to populate
                      serviceId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  settings:
                    description: |-
                      Advanced configuration options for the ClickPipe. These settings are specific to each pipe. For the complete list of available options, see the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger (search for the ClickPipes settings endpoint).
                    x-kubernetes-preserve-unknown-fields: true
                  source:
                    description: The data source for the ClickPipe. At
                      least one source configuration must be provided. (see below
                      for nested schema)
                    properties:
                      bigquery:
                        description: The BigQuery source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Google Cloud service account JSON key file content, base64 encoded.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - serviceAccountFileSecretRef
                            type: object
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel workers during initial load.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the BigQuery pipe. (`snapshot`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                            type: object
                          snapshotStagingPath:
                            description: |-
                              GCS bucket path for staging snapshot data (e.g., gs://my-bucket/staging/). Data will be automatically cleaned up after initial load.
                            type: string
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceDatasetName:
                                  description: |-
                                    Source BigQuery dataset name.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in BigQuery.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                        type: object
                      kafka:
                        description: The Kafka source configuration for
                          the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kafka source. (`PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`, `IAM_ROLE`, `IAM_USER`, `MUTUAL_TLS`). Default is `PLAIN`.
                            type: string
                          brokers:
                            description: |-
                              The list of Kafka bootstrap brokers. (comma separated)
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificates to validate the broker's certificate.
                            type: string
                          consumerGroup:
                            description: |-
                              <ID> will be used.
                              Consumer group of the Kafka source. If not provided `clickpipes-<ID>` will be used.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the Kafka source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              certificateSecretRef:
                                description: |-
                                  PEM encoded client certificate for mTLS authentication. Use with `MUTUAL_TLS` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              connectionStringSecretRef:
                                description: |-
                                  The connection string for the Kafka source. Use with `azureeventhub` Kafka source type. Use with `PLAIN` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordSecretRef:
                                description: |-
                                  The password for the Kafka source. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the Kafka source. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              privateKeySecretRef:
                                description: |-
                                  PEM encoded client private key for mTLS authentication. Use with `MUTUAL_TLS` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the Kafka source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              usernameSecretRef:
                                description: |-
                                  The username for the Kafka source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          exactlyOnce:
                            description: |-
                              Enable exactly-once delivery. Guarantees every Kafka record is inserted exactly once across restarts and rebalances.
                            type: boolean
                          format:
                            description: |-
                              The format of the Kafka source. (`JSONEachRow`, `Avro`, `AvroConfluent`, `Protobuf`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kafka source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kafka#iam
                            type: string
                          offset:
                            description: The Kafka offset. (see below
                              for nested schema)
                            properties:
                              strategy:
                                description: |-
                                  The offset strategy for the Kafka source. (`from_beginning`, `from_latest`, `from_timestamp`)
                                type: string
                              timestamp:
                                description: |-
                                  01-01T00:00)
                                  The timestamp for the Kafka offset. Use with `from_timestamp` offset strategy. (format `2021-01-01T00:00`)
                                type: string
                            type: object
                          reversePrivateEndpointIds:
                            description: |-
                              The list of reverse private endpoint IDs for the Kafka source. (comma separated)
                            items:
                              type: string
                            type: array
                          schemaRegistry:
                            description: The schema registry for the
                              Kafka source. (see below for nested schema)
                            properties:
                              authentication:
                                description: |-
                                  SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                                  The authentication method for the Schema Registry. Only supported is `PLAIN`.
                                type: string
                              credentials:
                                description: The credentials
                                  for BigQuery access. (see below for nested schema)
                                properties:
                                  passwordSecretRef:
                                    description: |-
                                      The password for the Schema Registry. Either `password` or `password_wo` must be provided.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                  passwordWoSecretRef:
                                    description: |-
                                      Write-only password for the Schema Registry. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                  passwordWoVersion:
                                    description: |-
                                      Version trigger for `password_wo`. Increment to push a new password to the API.
                                    type: number
                                  usernameSecretRef:
                                    description: |-
                                      The username for the Schema Registry.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        description: Name of the secret.
                                        type: string
                                      namespace:
                                        description: Namespace of the secret.
                                        type: string
                                    required:
                                    - key
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - usernameSecretRef
                                type: object
                              url:
                                description: |-
                                  The URL of the schema registry.
                                type: string
                            type: object
                          topics:
                            description: |-
                              The list of Kafka topics. (comma separated)
                            type: string
                          type:
                            description: |-
                              The type of the Kafka source. (`kafka`, `redpanda`, `confluent`, `msk`, `warpstream`, `azureeventhub`, `gcmk`). Default is `kafka`.
                            type: string
                        type: object
                      kinesis:
                        description: The Kinesis source configuration
                          for the ClickPipe. Only authentication, iam_role and access_key
                          can be updated in place; changing any other field forces
                          resource replacement (destroy and recreate). (see below
                          for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - accessKeyIdSecretRef
                            - secretKeySecretRef
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kinesis source. (`IAM_ROLE`, `IAM_USER`).
                            type: string
                          format:
                            description: |-
                              The format of the Kinesis source. (`JSONEachRow`, `Avro`, `AvroConfluent`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kinesis source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kinesis.
                            type: string
                          iteratorType:
                            description: |-
                              The iterator type for the Kinesis source. (`TRIM_HORIZON`, `LATEST`, `AT_TIMESTAMP`)
                            type: string
                          region:
                            description: |-
                              The AWS region of the Kinesis stream.
                            type: string
                          streamName:
                            description: |-
                              The name of the Kinesis stream.
                            type: string
                          timestamp:
                            description: |-
                              01-01T00:00)
                              The timestamp for the Kinesis source. Use with `AT_TIMESTAMP` iterator type. (format `2021-01-01T00:00`)
                            type: string
                          useEnhancedFanOut:
                            description: |-
                              Whether to use enhanced fan-out consumer.
                            type: boolean
                        type: object
                      mongodb:
                        description: The MongoDB CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MongoDB server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the MongoDB instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the MongoDB instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              username:
                                description: |-
                                  The username for the MongoDB instance.
                                type: string
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MongoDB connection. Defaults to false (TLS enabled).
                            type: boolean
                          readPreference:
                            description: |-
                              MongoDB read preference for replica set reads. (`primary`, `primaryPreferred`, `secondary`, `secondaryPreferred`, `nearest`)
                            type: string
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MongoDB DELETE operations.
                                type: boolean
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch during CDC replication.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the MongoDB pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows per partition during the snapshot phase.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of collections to snapshot in parallel during the initial load phase.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MongoDB during CDC replication.
                                type: number
                              useJsonNativeFormat:
                                description: |-
                                  Store JSON values in native ClickHouse JSON format. When disabled, JSON data is stored as String.
                                type: boolean
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                sourceCollection:
                                  description: |-
                                    MongoDB source collection name.
                                  type: string
                                sourceDatabaseName:
                                  description: |-
                                    MongoDB source database name.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    ClickHouse target table name. The table will be created automatically if it does not exist.
                                  type: string
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections.
                            type: string
                          uri:
                            description: |-
                              MongoDB connection URI. Supports both standard URIs (mongodb://...) and SRV URIs (mongodb+srv://...).
                            type: string
                        type: object
                      mysql:
                        description: The MySQL CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for MySQL connection. Supported values: `basic`, `IAM_ROLE`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MySQL server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the MySQL instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the MySQL instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              usernameSecretRef:
                                description: |-
                                  The username for the MySQL instance.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - usernameSecretRef
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MySQL connection.
                            type: boolean
                          host:
                            description: |-
                              The hostname of the MySQL instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `IAM_ROLE`.
                            type: string
                          port:
                            description: |-
                              The port of the MySQL instance. Default is 3306.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MySQL DELETE operations.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMechanism:
                                description: |-
                                  Replication mechanism for the MySQL pipe. (`GTID`, `FILE_POS`). Default is `GTID`. Mechanisms other than `GTID` (e.g. `FILE_POS`) must be enabled for your organization; contact ClickHouse support to enable this feature.
                                type: string
                              replicationMode:
                                description: |-
                                  Replication mode for the MySQL pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MySQL.
                                type: number
                              useCompression:
                                description: |-
                                  Enable compression for the MySQL replication connection.
                                type: boolean
                            type: object
                          skipCertVerification:
                            description: |-
                              Skip certificate verification for the MySQL connection.
                            type: boolean
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Must be an indexed column of integer, date, datetime, or timestamp type.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema (database) name in MySQL.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in MySQL.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of MySQL-compatible source. (`mysql`, `rdsmysql`, `auroramysql`, `planetscalevitess`, `mariadb`, `rdsmariadb`). Default is `mysql`.
                            type: string
                        type: object
                      objectStorage:
                        description: The compatible object storage source
                          configuration for the ClickPipe. (see below for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              CONNECTION_STRING is for Azure Blob Storage. IAM_ROLE and IAM_USER are for AWS S3. IAM_USER and SERVICE_ACCOUNT are for GCS. If not provided, no authentication is used
                            type: string
                          azureContainerName:
                            description: |-
                              Container name for Azure Blob Storage. Required when type is azureblobstorage. Example: `mycontainer`
                            type: string
                          compression:
                            description: |-
                              Compression algorithm used for the files.. (`none`, `auto`, `gzip`, `brotli`, `br`, `xz`, `LZMA`, `zstd`)
                            type: string
                          connectionStringSecretRef:
                            description: |-
                              Connection string for Azure Blob Storage authentication. Required when authentication is CONNECTION_STRING. Example: `DefaultEndpointsProtocol=https;AccountName=myaccount;AccountKey=mykey;EndpointSuffix=core.windows.net`
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          delimiter:
                            description: |-
                              The delimiter for the S3 source. Default is `,`.
                            type: string
                          format:
                            description: |-
                              The format of the S3 objects. (`JSONEachRow`, `CSV`, `CSVWithNames`, `Parquet`, `Avro`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the S3 source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#authentication
                            type: string
                          isContinuous:
                            description: |-
                              If set to true, the pipe will continuously read new files from the source. If set to false, the pipe will read the files only once. New files have to be uploaded lexically order.
                            type: boolean
                          path:
                            description: |-
                              Path to the file(s) within the Azure container. Used for Azure Blob Storage sources. You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations. Example: `data/logs/*.json`
                            type: string
                          queueUrl:
                            description: |-
                              Queue URL for event-based continuous ingestion. When provided, files are ingested based on event notifications rather than lexicographical order. Only applicable when `is_continuous` is `true` and authentication is provided. For S3: SQS URL in the format `https://sqs.{region}.amazonaws.com/{account-id}/{queue-name}`. For GCS: Pub/Sub subscription in the format `projects/{project}/subscriptions/{subscription}`.
                            type: string
                          serviceAccountKeySecretRef:
                            description: |-
                              Base64-encoded GCP service account JSON key for GCS authentication. Required when authentication is `SERVICE_ACCOUNT`.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          skipInitialLoad:
                            description: |-
                              If set to true, skips the initial load and only ingests files delivered by queue notifications. Only applicable when `queue_url` is provided.
                            type: boolean
                          startAfter:
                            description: |-
                              06-01/). Cannot be provided when skip_initial_load is true.
                              Skip all files up to and including this object key during the initial load (e.g. `events/2026-06-01/`). Cannot be provided when `skip_initial_load` is true.
                            type: string
                          type:
                            description: |-
                              The type of the S3-compatible source (`s3`, `gcs`, `azureblobstorage`). Default is `s3`.
                            type: string
                          url:
                            description: |-
                              The URL of the S3/GCS bucket. Required for S3 and GCS types. Not used for Azure Blob Storage (use path and azure_container_name instead). You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations
                            type: string
                        type: object
                      postgres:
                        description: The Postgres CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for Postgres connection. Supported values: `basic`, `iam_role`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the Postgres server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The password for the Postgres instance. Use `password_wo` instead to keep the value out of state.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoSecretRef:
                                description: |-
                                  Write-only password for the Postgres instance. Not persisted to state. Pair with `password_wo_version` to trigger updates.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              usernameSecretRef:
                                description: |-
                                  The username for the Postgres instance.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - usernameSecretRef
                            type: object
                          database:
                            description: |-
                              The database name of the Postgres instance.
                            type: string
                          host:
                            description: |-
                              The hostname of the Postgres instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `iam_role`.
                            type: string
                          port:
                            description: |-
                              The port of the Postgres instance. Default is 5432.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for PostgreSQL DELETE operations.
                                type: boolean
                              enableFailoverSlots:
                                description: |-
                                  Enable failover for created replication slot. Requires a replication slot to NOT be set.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              publicationName:
                                description: |-
                                  Publication name to use for replication. If not provided, ClickPipes will create one.
                                type: string
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the Postgres pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              replicationSlotName:
                                description: |-
                                  Replication slot name to use for replication. Only applicable when replication_mode is `cdc_only`.
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from Postgres.
                                type: number
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Only beneficial for PostgreSQL 13 (no benefit for PG14+, which supports indexed ctid scans). Must be an indexed column of type: `smallint`, `integer`, `bigint`, `timestamp without time zone`, or `timestamp with time zone`. Unrelated to ClickHouse partitioning.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema name in Postgres.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in Postgres.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of the Postgres source. (`postgres`, `supabase`, `neon`, `alloydb`, `planetscale`, `rdspostgres`, `aurorapostgres`, `cloudsqlpostgres`, `azurepostgres`, `crunchybridge`, `tigerdata`). Default is `postgres`.
                            type: string
                        type: object
                      pubsub:
                        description: The GCP Pub/Sub source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          ackDeadline:
                            description: |-
                              Acknowledgement deadline in seconds (10–600).
                            type: number
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Pub/Sub source. Currently only `SERVICE_ACCOUNT` is supported.
                            type: string
                          enableOrdering:
                            description: |-
                              Whether to enable ordered message delivery. Immutable — changing it requires destroy+create because ordered delivery is a property of the subscription at creation time.
                            type: boolean
                          filter:
                            description: |-
                              Optional Pub/Sub subscription filter expression (CEL). Max 256 characters. Immutable — changing it requires destroy+create because the underlying subscription filter cannot be edited in place.
                            type: string
                          format:
                            description: |-
                              The message format of the Pub/Sub topic. (`JSONEachRow`, `Avro`, `Protobuf`)
                            type: string
                          projectId:
                            description: |-
                              The GCP project ID that owns the Pub/Sub topic.
                            type: string
                          seekTimestamp:
                            description: |-
                              04-10T12:00:00Z). Required when seek_type = "timestamp"; must be omitted otherwise.
                              RFC 3339 timestamp (e.g. `2026-04-10T12:00:00Z`). Required when `seek_type = "timestamp"`; must be omitted otherwise.
                            type: string
                          seekType:
                            description: |-
                              The starting position for consuming the subscription. (`latest`, `earliest`, `timestamp`)
                            type: string
                          serviceAccountKey:
                            description: encoded GCP service account JSON key for
                              GCS authentication. Required when authentication is
                              SERVICE_ACCOUNT.
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Base64-encoded GCP service account JSON key file contents.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - serviceAccountFileSecretRef
                            type: object
                          topic:
                            description: |-
                              The Pub/Sub topic name (not the fully-qualified path).
                            type: string
                        type: object
                    type: object
                  stopped:
                    description: |-
                      Whether the ClickPipe should be stopped. Default is `false` (ClickPipe will be running). Cannot be set to `true` on creation — the ClickPipe must be created in a running state and then stopped via a subsequent apply.
                    type: boolean
                  triggerResync:
                    description: |-
                      Set to `true` to trigger a resync operation. Only applicable for Postgres pipes. Automatically resets to `false` after the resync is triggered.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.destination is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.destination)
                || (has(self.initProvider) && has(self.initProvider.destination))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
          status:
            description: ClickpipeStatus defines the observed state of Clickpipe.
            properties:
              atProvider:
                properties:
                  destination:
                    description: The destination for the ClickPipe. (see
                      below for nested schema)
                    properties:
                      columns:
                        description: The list of columns for the
                          ClickHouse table. Required for all sources except Postgres
                          CDC (where columns are determined from source tables). (see
                          below for nested schema)
                        items:
                          properties:
                            name:
                              description: |-
                                The name of the column.
                              type: string
                            type:
                              description: |-
                                The type of the column.
                              type: string
                          type: object
                        type: array
                      database:
                        description: |-
                          The name of the ClickHouse database. Default is `default`.
                        type: string
                      managedTable:
                        description: |-
                          Whether the table is managed by ClickHouse Cloud. If `false`, the table must exist in the database. Default is `true`. **Not applicable to database/CDC pipes** (Postgres, MySQL, BigQuery, MongoDB): for those sources destination tables are always managed per-table via `table_mappings`, so this field is ignored and not sent to the API.
                        type: boolean
                      roles:
                        description: |-
                          ClickPipe will create a ClickHouse user with these roles. Add your custom roles here if required.
                        items:
                          type: string
                        type: array
                      table:
                        description: |-
                          The name of the ClickHouse table. Required for all sources except Postgres CDC (where tables are created from table_mappings).
                        type: string
                      tableDefinition:
                        description: table via table_mappings (each mapping's target_table,
                          table_engine, sorting_keys, etc.), so configuring this is
                          rejected at plan time. (see below for nested schema)
                        properties:
                          engine:
                            description: The engine of the ClickHouse
                              table. (see below for nested schema)
                            properties:
                              columnIds:
                                description: |-
                                  Column IDs to sum for SummingMergeTree engine. Required when engine type is `SummingMergeTree`.
                                items:
                                  type: string
                                type: array
                              type:
                                description: |-
                                  The type of the engine. Supported engines: `MergeTree`, `ReplacingMergeTree`, `SummingMergeTree`, `Null`.
                                type: string
                              versionColumnId:
                                description: |-
                                  Column ID to use as version for ReplacingMergeTree engine. Required when engine type is `ReplacingMergeTree`.
                                type: string
                            type: object
                          partitionBy:
                            description: |-
                              The column to partition the table by.
                            type: string
                          primaryKey:
                            description: |-
                              The primary key of the table.
                            type: string
                          sortingKey:
                            description: |-
                              The list of columns for the sorting key.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  fieldMappings:
                    description: Field mapping between source and
                      destination table. (see below for nested schema)
                    items:
                      properties:
                        destinationField:
                          description: |-
                            The name of the column in destination table.
                          type: string
                        sourceField:
                          description: |-
                            The name of the source field.
                          type: string
                      type: object
                    type: array
                  id:
                    description: The ID of the ClickPipe. Generated by the
                      ClickHouse Cloud.
                    type: string
                  name:
                    description: |-
                      The name of the ClickPipe.
                    type: string
                  scaling:
                    description: (see below for nested schema)
                    properties:
                      replicaCpuMillicores:
                        description: |-
                          The CPU allocation per replica in millicores. Must be between 125 and 2000.
                        type: number
                      replicaMemoryGb:
                        description: |-
                          The memory allocation per replica in GB. Must be between 0.5 and 8.0.
                        type: number
                      replicas:
                        description: |-
                          The number of desired replicas for the ClickPipe. Default is 1. The maximum value is 10.
                        type: number
                    type: object
                  serviceId:
                    description: |-
                      The ID of the service to which the ClickPipe belongs.
                    type: string
                  settings:
                    description: |-
                      Advanced configuration options for the ClickPipe. These settings are specific to each pipe. For the complete list of available options, see the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger (search for the ClickPipes settings endpoint).
                    x-kubernetes-preserve-unknown-fields: true
                  source:
                    description: The data source for the ClickPipe. At
                      least one source configuration must be provided. (see below
                      for nested schema)
                    properties:
                      bigquery:
                        description: The BigQuery source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Google Cloud service account JSON key file content, base64 encoded.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel workers during initial load.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the BigQuery pipe. (`snapshot`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                            type: object
                          snapshotStagingPath:
                            description: |-
                              GCS bucket path for staging snapshot data (e.g., gs://my-bucket/staging/). Data will be automatically cleaned up after initial load.
                            type: string
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceDatasetName:
                                  description: |-
                                    Source BigQuery dataset name.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in BigQuery.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                        type: object
                      kafka:
                        description: The Kafka source configuration for
                          the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kafka source. (`PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`, `IAM_ROLE`, `IAM_USER`, `MUTUAL_TLS`). Default is `PLAIN`.
                            type: string
                          brokers:
                            description: |-
                              The list of Kafka bootstrap brokers. (comma separated)
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificates to validate the broker's certificate.
                            type: string
                          consumerGroup:
                            description: |-
                              <ID> will be used.
                              Consumer group of the Kafka source. If not provided `clickpipes-<ID>` will be used.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                            type: object
                          exactlyOnce:
                            description: |-
                              Enable exactly-once delivery. Guarantees every Kafka record is inserted exactly once across restarts and rebalances.
                            type: boolean
                          format:
                            description: |-
                              The format of the Kafka source. (`JSONEachRow`, `Avro`, `AvroConfluent`, `Protobuf`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kafka source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kafka#iam
                            type: string
                          offset:
                            description: The Kafka offset. (see below
                              for nested schema)
                            properties:
                              strategy:
                                description: |-
                                  The offset strategy for the Kafka source. (`from_beginning`, `from_latest`, `from_timestamp`)
                                type: string
                              timestamp:
                                description: |-
                                  01-01T00:00)
                                  The timestamp for the Kafka offset. Use with `from_timestamp` offset strategy. (format `2021-01-01T00:00`)
                                type: string
                            type: object
                          reversePrivateEndpointIds:
                            description: |-
                              The list of reverse private endpoint IDs for the Kafka source. (comma separated)
                            items:
                              type: string
                            type: array
                          schemaRegistry:
                            description: The schema registry for the
                              Kafka source. (see below for nested schema)
                            properties:
                              authentication:
                                description: |-
                                  SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                                  The authentication method for the Schema Registry. Only supported is `PLAIN`.
                                type: string
                              credentials:
                                description: The credentials
                                  for BigQuery access. (see below for nested schema)
                                properties:
                                  passwordWoVersion:
                                    description: |-
                                      Version trigger for `password_wo`. Increment to push a new password to the API.
                                    type: number
                                type: object
                              url:
                                description: |-
                                  The URL of the schema registry.
                                type: string
                            type: object
                          topics:
                            description: |-
                              The list of Kafka topics. (comma separated)
                            type: string
                          type:
                            description: |-
                              The type of the Kafka source. (`kafka`, `redpanda`, `confluent`, `msk`, `warpstream`, `azureeventhub`, `gcmk`). Default is `kafka`.
                            type: string
                        type: object
                      kinesis:
                        description: The Kinesis source configuration
                          for the ClickPipe. Only authentication, iam_role and access_key
                          can be updated in place; changing any other field forces
                          resource replacement (destroy and recreate). (see below
                          for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the Kinesis source.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Kinesis source. (`IAM_ROLE`, `IAM_USER`).
                            type: string
                          format:
                            description: |-
                              The format of the Kinesis source. (`JSONEachRow`, `Avro`, `AvroConfluent`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the Kinesis source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/kinesis.
                            type: string
                          iteratorType:
                            description: |-
                              The iterator type for the Kinesis source. (`TRIM_HORIZON`, `LATEST`, `AT_TIMESTAMP`)
                            type: string
                          region:
                            description: |-
                              The AWS region of the Kinesis stream.
                            type: string
                          streamName:
                            description: |-
                              The name of the Kinesis stream.
                            type: string
                          timestamp:
                            description: |-
                              01-01T00:00)
                              The timestamp for the Kinesis source. Use with `AT_TIMESTAMP` iterator type. (format `2021-01-01T00:00`)
                            type: string
                          useEnhancedFanOut:
                            description: |-
                              Whether to use enhanced fan-out consumer.
                            type: boolean
                        type: object
                      mongodb:
                        description: The MongoDB CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MongoDB server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                              username:
                                description: |-
                                  The username for the MongoDB instance.
                                type: string
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MongoDB connection. Defaults to false (TLS enabled).
                            type: boolean
                          readPreference:
                            description: |-
                              MongoDB read preference for replica set reads. (`primary`, `primaryPreferred`, `secondary`, `secondaryPreferred`, `nearest`)
                            type: string
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MongoDB DELETE operations.
                                type: boolean
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch during CDC replication.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the MongoDB pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows per partition during the snapshot phase.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of collections to snapshot in parallel during the initial load phase.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MongoDB during CDC replication.
                                type: number
                              useJsonNativeFormat:
                                description: |-
                                  Store JSON values in native ClickHouse JSON format. When disabled, JSON data is stored as String.
                                type: boolean
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                sourceCollection:
                                  description: |-
                                    MongoDB source collection name.
                                  type: string
                                sourceDatabaseName:
                                  description: |-
                                    MongoDB source database name.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    ClickHouse target table name. The table will be created automatically if it does not exist.
                                  type: string
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections.
                            type: string
                          uri:
                            description: |-
                              MongoDB connection URI. Supports both standard URIs (mongodb://...) and SRV URIs (mongodb+srv://...).
                            type: string
                        type: object
                      mysql:
                        description: The MySQL CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for MySQL connection. Supported values: `basic`, `IAM_ROLE`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the MySQL server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                            type: object
                          disableTls:
                            description: |-
                              Disable TLS for the MySQL connection.
                            type: boolean
                          host:
                            description: |-
                              The hostname of the MySQL instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `IAM_ROLE`.
                            type: string
                          port:
                            description: |-
                              The port of the MySQL instance. Default is 3306.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for MySQL DELETE operations.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMechanism:
                                description: |-
                                  Replication mechanism for the MySQL pipe. (`GTID`, `FILE_POS`). Default is `GTID`. Mechanisms other than `GTID` (e.g. `FILE_POS`) must be enabled for your organization; contact ClickHouse support to enable this feature.
                                type: string
                              replicationMode:
                                description: |-
                                  Replication mode for the MySQL pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from MySQL.
                                type: number
                              useCompression:
                                description: |-
                                  Enable compression for the MySQL replication connection.
                                type: boolean
                            type: object
                          skipCertVerification:
                            description: |-
                              Skip certificate verification for the MySQL connection.
                            type: boolean
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Must be an indexed column of integer, date, datetime, or timestamp type.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema (database) name in MySQL.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in MySQL.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of MySQL-compatible source. (`mysql`, `rdsmysql`, `auroramysql`, `planetscalevitess`, `mariadb`, `rdsmariadb`). Default is `mysql`.
                            type: string
                        type: object
                      objectStorage:
                        description: The compatible object storage source
                          configuration for the ClickPipe. (see below for nested schema)
                        properties:
                          accessKey:
                            description: The access key for the Kinesis
                              source. Use with IAM_USER authentication. Can be rotated
                              in place via an update. (see below for nested schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  The access key ID for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretKeySecretRef:
                                description: |-
                                  The secret key for the S3 source. Use with `IAM_USER` authentication.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              CONNECTION_STRING is for Azure Blob Storage. IAM_ROLE and IAM_USER are for AWS S3. IAM_USER and SERVICE_ACCOUNT are for GCS. If not provided, no authentication is used
                            type: string
                          azureContainerName:
                            description: |-
                              Container name for Azure Blob Storage. Required when type is azureblobstorage. Example: `mycontainer`
                            type: string
                          compression:
                            description: |-
                              Compression algorithm used for the files.. (`none`, `auto`, `gzip`, `brotli`, `br`, `xz`, `LZMA`, `zstd`)
                            type: string
                          delimiter:
                            description: |-
                              The delimiter for the S3 source. Default is `,`.
                            type: string
                          format:
                            description: |-
                              The format of the S3 objects. (`JSONEachRow`, `CSV`, `CSVWithNames`, `Parquet`, `Avro`)
                            type: string
                          iamRole:
                            description: |-
                              The IAM role for the S3 source. Use with `IAM_ROLE` authentication. It can be used with AWS ClickHouse service only. Read more at https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#authentication
                            type: string
                          isContinuous:
                            description: |-
                              If set to true, the pipe will continuously read new files from the source. If set to false, the pipe will read the files only once. New files have to be uploaded lexically order.
                            type: boolean
                          path:
                            description: |-
                              Path to the file(s) within the Azure container. Used for Azure Blob Storage sources. You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations. Example: `data/logs/*.json`
                            type: string
                          queueUrl:
                            description: |-
                              Queue URL for event-based continuous ingestion. When provided, files are ingested based on event notifications rather than lexicographical order. Only applicable when `is_continuous` is `true` and authentication is provided. For S3: SQS URL in the format `https://sqs.{region}.amazonaws.com/{account-id}/{queue-name}`. For GCS: Pub/Sub subscription in the format `projects/{project}/subscriptions/{subscription}`.
                            type: string
                          skipInitialLoad:
                            description: |-
                              If set to true, skips the initial load and only ingests files delivered by queue notifications. Only applicable when `queue_url` is provided.
                            type: boolean
                          startAfter:
                            description: |-
                              06-01/). Cannot be provided when skip_initial_load is true.
                              Skip all files up to and including this object key during the initial load (e.g. `events/2026-06-01/`). Cannot be provided when `skip_initial_load` is true.
                            type: string
                          type:
                            description: |-
                              The type of the S3-compatible source (`s3`, `gcs`, `azureblobstorage`). Default is `s3`.
                            type: string
                          url:
                            description: |-
                              The URL of the S3/GCS bucket. Required for S3 and GCS types. Not used for Azure Blob Storage (use path and azure_container_name instead). You can specify multiple files using bash-like wildcards. For more information, see the documentation on using wildcards in path: https://clickhouse.com/docs/en/integrations/clickpipes/object-storage#limitations
                            type: string
                        type: object
                      postgres:
                        description: The Postgres CDC source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              Authentication method for Postgres connection. Supported values: `basic`, `iam_role`. Default is `basic`.
                            type: string
                          caCertificate:
                            description: |-
                              PEM encoded CA certificate to validate the Postgres server certificate.
                            type: string
                          credentials:
                            description: The credentials for
                              BigQuery access. (see below for nested schema)
                            properties:
                              passwordWoVersion:
                                description: |-
                                  Version trigger for `password_wo`. Increment to push a new password to the API.
                                type: number
                            type: object
                          database:
                            description: |-
                              The database name of the Postgres instance.
                            type: string
                          host:
                            description: |-
                              The hostname of the Postgres instance.
                            type: string
                          iamRole:
                            description: |-
                              IAM role ARN for IAM authentication. Required when authentication is set to `iam_role`.
                            type: string
                          port:
                            description: |-
                              The port of the Postgres instance. Default is 5432.
                            type: number
                          settings:
                            description: Advanced configuration options
                              for the ClickPipe. These settings are specific to each
                              pipe. For the complete list of available options, see
                              the OpenAPI documentation at https://clickhouse.com/docs/cloud/manage/api/swagger
                              (search for the ClickPipes settings endpoint).
                            properties:
                              allowNullableColumns:
                                description: |-
                                  Allow nullable columns in the destination table.
                                type: boolean
                              deleteOnMerge:
                                description: |-
                                  Enable hard delete behavior in ReplacingMergeTree for PostgreSQL DELETE operations.
                                type: boolean
                              enableFailoverSlots:
                                description: |-
                                  Enable failover for created replication slot. Requires a replication slot to NOT be set.
                                type: boolean
                              initialLoadParallelism:
                                description: |-
                                  Number of parallel connections to use during initial load.
                                type: number
                              publicationName:
                                description: |-
                                  Publication name to use for replication. If not provided, ClickPipes will create one.
                                type: string
                              pullBatchSize:
                                description: |-
                                  Number of rows to pull in each batch.
                                type: number
                              replicationMode:
                                description: |-
                                  Replication mode for the Postgres pipe. (`cdc`, `snapshot`, `cdc_only`)
                                type: string
                              replicationSlotName:
                                description: |-
                                  Replication slot name to use for replication. Only applicable when replication_mode is `cdc_only`.
                                type: string
                              snapshotNumRowsPerPartition:
                                description: |-
                                  Number of rows to snapshot per partition.
                                type: number
                              snapshotNumberOfParallelTables:
                                description: |-
                                  Number of parallel tables to snapshot.
                                type: number
                              syncIntervalSeconds:
                                description: |-
                                  Interval in seconds to sync data from Postgres.
                                type: number
                            type: object
                          tableMappings:
                            description: Table mappings from BigQuery
                              source to ClickHouse destination. (see below for nested
                              schema)
                            items:
                              properties:
                                excludedColumns:
                                  description: |-
                                    Columns to exclude from replication.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: set
                                partitionKey:
                                  description: |-
                                    Custom partitioning column used for parallel snapshotting. Only beneficial for PostgreSQL 13 (no benefit for PG14+, which supports indexed ctid scans). Must be an indexed column of type: `smallint`, `integer`, `bigint`, `timestamp without time zone`, or `timestamp with time zone`. Unrelated to ClickHouse partitioning.
                                  type: string
                                sortingKeys:
                                  description: |-
                                    Ordered list of columns to use as sorting key for the target table. Required when use_custom_sorting_key is true.
                                  items:
                                    type: string
                                  type: array
                                sourceSchemaName:
                                  description: |-
                                    Source schema name in Postgres.
                                  type: string
                                sourceTable:
                                  description: |-
                                    Source table name in Postgres.
                                  type: string
                                tableEngine:
                                  description: |-
                                    Table engine to use for the target table. (`MergeTree`, `ReplacingMergeTree`, `Null`)
                                  type: string
                                targetTable:
                                  description: |-
                                    Target table name in ClickHouse.
                                  type: string
                                useCustomSortingKey:
                                  description: |-
                                    Whether to use a custom sorting key for the target table.
                                  type: boolean
                              type: object
                            type: array
                          tlsHost:
                            description: |-
                              TLS/SSL host for secure connections. Used to verify the server certificate.
                            type: string
                          type:
                            description: |-
                              The type of the Postgres source. (`postgres`, `supabase`, `neon`, `alloydb`, `planetscale`, `rdspostgres`, `aurorapostgres`, `cloudsqlpostgres`, `azurepostgres`, `crunchybridge`, `tigerdata`). Default is `postgres`.
                            type: string
                        type: object
                      pubsub:
                        description: The GCP Pub/Sub source configuration
                          for the ClickPipe. (see below for nested schema)
                        properties:
                          ackDeadline:
                            description: |-
                              Acknowledgement deadline in seconds (10–600).
                            type: number
                          authentication:
                            description: |-
                              SHA-256, SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS). Default is PLAIN.
                              The authentication method for the Pub/Sub source. Currently only `SERVICE_ACCOUNT` is supported.
                            type: string
                          enableOrdering:
                            description: |-
                              Whether to enable ordered message delivery. Immutable — changing it requires destroy+create because ordered delivery is a property of the subscription at creation time.
                            type: boolean
                          filter:
                            description: |-
                              Optional Pub/Sub subscription filter expression (CEL). Max 256 characters. Immutable — changing it requires destroy+create because the underlying subscription filter cannot be edited in place.
                            type: string
                          format:
                            description: |-
                              The message format of the Pub/Sub topic. (`JSONEachRow`, `Avro`, `Protobuf`)
                            type: string
                          projectId:
                            description: |-
                              The GCP project ID that owns the Pub/Sub topic.
                            type: string
                          seekTimestamp:
                            description: |-
                              04-10T12:00:00Z). Required when seek_type = "timestamp"; must be omitted otherwise.
                              RFC 3339 timestamp (e.g. `2026-04-10T12:00:00Z`). Required when `seek_type = "timestamp"`; must be omitted otherwise.
                            type: string
                          seekType:
                            description: |-
                              The starting position for consuming the subscription. (`latest`, `earliest`, `timestamp`)
                            type: string
                          serviceAccountKey:
                            description: encoded GCP service account JSON key for
                              GCS authentication. Required when authentication is
                              SERVICE_ACCOUNT.
                            properties:
                              serviceAccountFileSecretRef:
                                description: |-
                                  Base64-encoded GCP service account JSON key file contents.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          topic:
                            description: |-
                              The Pub/Sub topic name (not the fully-qualified path).
                            type: string
                        type: object
                    type: object
                  state:
                    description: |-
                      The current state of the ClickPipe. This is a read-only field that reports the actual state from ClickHouse Cloud. Possible values include `Running`, `Stopped`, `Paused`, `Provisioning`, `Failed`, `InternalError`, etc.
                    type: string
                  stopped:
                    description: |-
                      Whether the ClickPipe should be stopped. Default is `false` (ClickPipe will be running). Cannot be set to `true` on creation — the ClickPipe must be created in a running state and then stopped via a subsequent apply.
                    type: boolean
                  triggerResync:
                    description: |-
                      Set to `true` to trigger a resync operation. Only applicable for Postgres pipes. Automatically resets to `false` after the resync is triggered.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
