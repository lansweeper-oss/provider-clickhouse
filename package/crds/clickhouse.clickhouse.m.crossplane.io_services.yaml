---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: services.clickhouse.clickhouse.m.crossplane.io
spec:
  group: clickhouse.clickhouse.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - clickhouse
    kind: Service
    listKind: ServiceList
    plural: services
    singular: service
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Service is the Schema for the Services API. You can use the
          clickhouse_service resource to deploy ClickHouse cloud instances on supported
          cloud providers. Known limitations: If you create a service with warehouse_id
          set and then remove warehouse_id attribute completely, the provider won''t
          detect the change. If you want to make a secondary service become primary,
          remove the warehouse_id and taint it before applying.If you create a service
          with readonly flag set to true and then remove readonly flag completely,
          the provider won''t detect the change. If you want to make a secondary service
          read write, explicitly set the readonly flag to false.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceSpec defines the desired state of Service
            properties:
              forProvider:
                properties:
                  backupConfiguration:
                    description: Configuration of service backup settings.
                      (see below for nested schema)
                    properties:
                      backupPeriodInHours:
                        description: |-
                          Interval in hours between each backup.
                        type: number
                      backupRetentionPeriodInHours:
                        description: |-
                          How long in hours to keep a backup before deleting it.
                        type: number
                      backupStartTime:
                        description: |-
                          Time of the day in UTC that indicates the start time of a 2 hours window to be used for backup. If set, backup_period_in_hours must be null and backups are created once a day.
                        type: string
                    type: object
                  backupId:
                    description: |-
                      ID of the backup to restore when creating new service. If specified, the service will be created as a restore operation
                    type: string
                  byocId:
                    description: |-
                      BYOC ID related to the cloud provider account you want to create this service into.
                    type: string
                  cloudProvider:
                    description: |-
                      Cloud provider ('aws', 'gcp', or 'azure') in which the service is deployed in.
                    type: string
                  complianceType:
                    description: |-
                      Compliance type of the service. Can be 'hipaa', 'pci'. Required for organizations that wish to deploy their services in the hipaa/pci compliant environment. NOTE: hipaa/pci compliance should be enabled for your ClickHouse organization before using this field.
                    type: string
                  doubleSha1PasswordHashSecretRef:
                    description: |-
                      Double SHA1 hash of password for connecting with the MySQL protocol. Cannot be specified if `password` or `password_wo` is specified.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  enableCoreDumps:
                    description: |-
                      Enable core dumps for the service.
                    type: boolean
                  encryptionAssumedRoleIdentifier:
                    description: |-
                      Custom role identifier ARN.
                    type: string
                  encryptionKey:
                    description: |-
                      Custom encryption key ARN.
                    type: string
                  endpoints:
                    description: Allow to enable and configure additional
                      endpoints (read protocols) to expose on the ClickHouse service.
                      (see below for nested schema)
                    properties:
                      https:
                        description: (see below for nested schema)
                        type: object
                      mysql:
                        description: (see below for nested schema)
                        properties:
                          enabled:
                            description: |-
                              Wether to enable the mysql endpoint or not.
                            type: boolean
                        type: object
                      nativesecure:
                        description: (see below for nested schema)
                        type: object
                    type: object
                  idleScaling:
                    description: |-
                      When set to true the service is allowed to scale down to zero when idle.
                    type: boolean
                  idleTimeoutMinutes:
                    description: |-
                      Set minimum idling timeout (in minutes). Must be greater than or equal to 5 minutes. Must be set if idle_scaling is enabled.
                    type: number
                  ipAccess:
                    description: List of IP addresses allowed to
                      access the service. (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            Description of the IP address.
                          type: string
                        source:
                          description: |-
                            IP address allowed to access the service. In case you want to set the ip_access to anywhere you should set source to 0.0.0.0/0
                          type: string
                      type: object
                    type: array
                  maxReplicaMemoryGb:
                    description: |-
                      Maximum memory of a single replica during auto-scaling in GiB.
                    type: number
                  maxTotalMemoryGb:
                    description: |-
                      Maximum total memory of all workers during auto-scaling in GiB.
                    type: number
                  minReplicaMemoryGb:
                    description: |-
                      Minimum memory of a single replica during auto-scaling in GiB.
                    type: number
                  minTotalMemoryGb:
                    description: |-
                      Minimum total memory of all workers during auto-scaling in GiB.
                    type: number
                  name:
                    description: |-
                      User defined identifier for the service.
                    type: string
                  numReplicas:
                    description: |-
                      Number of replicas for the service.
                    type: number
                  passwordHashSecretRef:
                    description: |-
                      SHA256 hash of password for the default user. One of either `password`, `password_wo`, or `password_hash` must be specified.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordSecretRef:
                    description: |-
                      Password for the default ClickHouse user.
                      When passwordSecretRef is set, that secret is used (Bring Your Own Password).
                      Otherwise a password is auto-generated and written to writeConnectionSecretToRef.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordWoSecretRef:
                    description: |-
                      Password for the default user (write-only, not persisted to state).
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordWoVersion:
                    description: |-
                      Version number for password_wo. Increment this to trigger a password update when using password_wo.
                    type: number
                  queryApiEndpoints:
                    description: Configuration of the query API endpoints
                      feature. (see below for nested schema)
                    properties:
                      allowedOrigins:
                        description: |-
                          Comma separated list of domain names to be allowed cross-origin resource sharing (CORS) access to the query API. Leave this field empty to restrict access to backend servers only
                        type: string
                      apiKeyIds:
                        description: |-
                          The UUIDs of the API Keys to grant access to the query API.
                        items:
                          type: string
                        type: array
                      roles:
                        description: |-
                          The Database role that will be used to run the query.
                        items:
                          type: string
                        type: array
                    type: object
                  readonly:
                    description: |-
                      Indicates if this service should be read only. Only allowed for secondary services, those which share data with another service (i.e. when `warehouse_id` field is set).
                    type: boolean
                  region:
                    description: |-
                      Region within the cloud provider in which the service is deployed in.
                    type: string
                  releaseChannel:
                    description: |-
                      Release channel to use for this service. Can be 'default', 'fast' or 'slow'.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags associated with the service as key-value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  tier:
                    description: |-
                      Tier of the service: 'development', 'production'. Required for organizations using the Legacy ClickHouse Cloud Tiers, must be omitted for organizations using the new ClickHouse Cloud Tiers.
                    type: string
                  transparentDataEncryption:
                    description: Configuration of the Transparent Data
                      Encryption (TDE) feature. Requires an organization with the
                      Enterprise plan. (see below for nested schema)
                    properties:
                      enabled:
                        description: |-
                          If true, TDE is enabled for the service.
                        type: boolean
                    type: object
                  warehouseId:
                    description: |-
                      Set it to the 'warehouse_id' attribute of another service to share the data with it. The service must be in the same cloud and region.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  backupConfiguration:
                    description: Configuration of service backup settings.
                      (see below for nested schema)
                    properties:
                      backupPeriodInHours:
                        description: |-
                          Interval in hours between each backup.
                        type: number
                      backupRetentionPeriodInHours:
                        description: |-
                          How long in hours to keep a backup before deleting it.
                        type: number
                      backupStartTime:
                        description: |-
                          Time of the day in UTC that indicates the start time of a 2 hours window to be used for backup. If set, backup_period_in_hours must be null and backups are created once a day.
                        type: string
                    type: object
                  backupId:
                    description: |-
                      ID of the backup to restore when creating new service. If specified, the service will be created as a restore operation
                    type: string
                  byocId:
                    description: |-
                      BYOC ID related to the cloud provider account you want to create this service into.
                    type: string
                  cloudProvider:
                    description: |-
                      Cloud provider ('aws', 'gcp', or 'azure') in which the service is deployed in.
                    type: string
                  complianceType:
                    description: |-
                      Compliance type of the service. Can be 'hipaa', 'pci'. Required for organizations that wish to deploy their services in the hipaa/pci compliant environment. NOTE: hipaa/pci compliance should be enabled for your ClickHouse organization before using this field.
                    type: string
                  doubleSha1PasswordHashSecretRef:
                    description: |-
                      Double SHA1 hash of password for connecting with the MySQL protocol. Cannot be specified if `password` or `password_wo` is specified.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  enableCoreDumps:
                    description: |-
                      Enable core dumps for the service.
                    type: boolean
                  encryptionAssumedRoleIdentifier:
                    description: |-
                      Custom role identifier ARN.
                    type: string
                  encryptionKey:
                    description: |-
                      Custom encryption key ARN.
                    type: string
                  endpoints:
                    description: Allow to enable and configure additional
                      endpoints (read protocols) to expose on the ClickHouse service.
                      (see below for nested schema)
                    properties:
                      https:
                        description: (see below for nested schema)
                        type: object
                      mysql:
                        description: (see below for nested schema)
                        properties:
                          enabled:
                            description: |-
                              Wether to enable the mysql endpoint or not.
                            type: boolean
                        type: object
                      nativesecure:
                        description: (see below for nested schema)
                        type: object
                    type: object
                  idleScaling:
                    description: |-
                      When set to true the service is allowed to scale down to zero when idle.
                    type: boolean
                  idleTimeoutMinutes:
                    description: |-
                      Set minimum idling timeout (in minutes). Must be greater than or equal to 5 minutes. Must be set if idle_scaling is enabled.
                    type: number
                  ipAccess:
                    description: List of IP addresses allowed to
                      access the service. (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            Description of the IP address.
                          type: string
                        source:
                          description: |-
                            IP address allowed to access the service. In case you want to set the ip_access to anywhere you should set source to 0.0.0.0/0
                          type: string
                      type: object
                    type: array
                  maxReplicaMemoryGb:
                    description: |-
                      Maximum memory of a single replica during auto-scaling in GiB.
                    type: number
                  maxTotalMemoryGb:
                    description: |-
                      Maximum total memory of all workers during auto-scaling in GiB.
                    type: number
                  minReplicaMemoryGb:
                    description: |-
                      Minimum memory of a single replica during auto-scaling in GiB.
                    type: number
                  minTotalMemoryGb:
                    description: |-
                      Minimum total memory of all workers during auto-scaling in GiB.
                    type: number
                  name:
                    description: |-
                      User defined identifier for the service.
                    type: string
                  numReplicas:
                    description: |-
                      Number of replicas for the service.
                    type: number
                  passwordHashSecretRef:
                    description: |-
                      SHA256 hash of password for the default user. One of either `password`, `password_wo`, or `password_hash` must be specified.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordSecretRef:
                    description: |-
                      Password for the default ClickHouse user.
                      When passwordSecretRef is set, that secret is used (Bring Your Own Password).
                      Otherwise a password is auto-generated and written to writeConnectionSecretToRef.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordWoSecretRef:
                    description: |-
                      Password for the default user (write-only, not persisted to state).
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  passwordWoVersion:
                    description: |-
                      Version number for password_wo. Increment this to trigger a password update when using password_wo.
                    type: number
                  queryApiEndpoints:
                    description: Configuration of the query API endpoints
                      feature. (see below for nested schema)
                    properties:
                      allowedOrigins:
                        description: |-
                          Comma separated list of domain names to be allowed cross-origin resource sharing (CORS) access to the query API. Leave this field empty to restrict access to backend servers only
                        type: string
                      apiKeyIds:
                        description: |-
                          The UUIDs of the API Keys to grant access to the query API.
                        items:
                          type: string
                        type: array
                      roles:
                        description: |-
                          The Database role that will be used to run the query.
                        items:
                          type: string
                        type: array
                    type: object
                  readonly:
                    description: |-
                      Indicates if this service should be read only. Only allowed for secondary services, those which share data with another service (i.e. when `warehouse_id` field is set).
                    type: boolean
                  region:
                    description: |-
                      Region within the cloud provider in which the service is deployed in.
                    type: string
                  releaseChannel:
                    description: |-
                      Release channel to use for this service. Can be 'default', 'fast' or 'slow'.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags associated with the service as key-value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  tier:
                    description: |-
                      Tier of the service: 'development', 'production'. Required for organizations using the Legacy ClickHouse Cloud Tiers, must be omitted for organizations using the new ClickHouse Cloud Tiers.
                    type: string
                  transparentDataEncryption:
                    description: Configuration of the Transparent Data
                      Encryption (TDE) feature. Requires an organization with the
                      Enterprise plan. (see below for nested schema)
                    properties:
                      enabled:
                        description: |-
                          If true, TDE is enabled for the service.
                        type: boolean
                    type: object
                  warehouseId:
                    description: |-
                      Set it to the 'warehouse_id' attribute of another service to share the data with it. The service must be in the same cloud and region.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudProvider is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudProvider)
                || (has(self.initProvider) && has(self.initProvider.cloudProvider))'
            - message: spec.forProvider.ipAccess is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ipAccess)
                || (has(self.initProvider) && has(self.initProvider.ipAccess))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: ServiceStatus defines the observed state of Service.
            properties:
              atProvider:
                properties:
                  backupConfiguration:
                    description: Configuration of service backup settings.
                      (see below for nested schema)
                    properties:
                      backupPeriodInHours:
                        description: |-
                          Interval in hours between each backup.
                        type: number
                      backupRetentionPeriodInHours:
                        description: |-
                          How long in hours to keep a backup before deleting it.
                        type: number
                      backupStartTime:
                        description: |-
                          Time of the day in UTC that indicates the start time of a 2 hours window to be used for backup. If set, backup_period_in_hours must be null and backups are created once a day.
                        type: string
                    type: object
                  backupId:
                    description: |-
                      ID of the backup to restore when creating new service. If specified, the service will be created as a restore operation
                    type: string
                  byocId:
                    description: |-
                      BYOC ID related to the cloud provider account you want to create this service into.
                    type: string
                  cloudProvider:
                    description: |-
                      Cloud provider ('aws', 'gcp', or 'azure') in which the service is deployed in.
                    type: string
                  complianceType:
                    description: |-
                      Compliance type of the service. Can be 'hipaa', 'pci'. Required for organizations that wish to deploy their services in the hipaa/pci compliant environment. NOTE: hipaa/pci compliance should be enabled for your ClickHouse organization before using this field.
                    type: string
                  enableCoreDumps:
                    description: |-
                      Enable core dumps for the service.
                    type: boolean
                  encryptionAssumedRoleIdentifier:
                    description: |-
                      Custom role identifier ARN.
                    type: string
                  encryptionKey:
                    description: |-
                      Custom encryption key ARN.
                    type: string
                  endpoints:
                    description: Allow to enable and configure additional
                      endpoints (read protocols) to expose on the ClickHouse service.
                      (see below for nested schema)
                    properties:
                      https:
                        description: (see below for nested schema)
                        properties:
                          host:
                            description: |-
                              Endpoint host.
                            type: string
                          port:
                            description: |-
                              Endpoint port.
                            type: number
                        type: object
                      mysql:
                        description: (see below for nested schema)
                        properties:
                          enabled:
                            description: |-
                              Wether to enable the mysql endpoint or not.
                            type: boolean
                          host:
                            description: |-
                              Endpoint host.
                            type: string
                          port:
                            description: |-
                              Endpoint port.
                            type: number
                        type: object
                      nativesecure:
                        description: (see below for nested schema)
                        properties:
                          host:
                            description: |-
                              Endpoint host.
                            type: string
                          port:
                            description: |-
                              Endpoint port.
                            type: number
                        type: object
                    type: object
                  iamRole:
                    description: |-
                      IAM role used for accessing objects in s3.
                    type: string
                  id:
                    description: ID of the created service. Generated by
                      ClickHouse Cloud.
                    type: string
                  idleScaling:
                    description: |-
                      When set to true the service is allowed to scale down to zero when idle.
                    type: boolean
                  idleTimeoutMinutes:
                    description: |-
                      Set minimum idling timeout (in minutes). Must be greater than or equal to 5 minutes. Must be set if idle_scaling is enabled.
                    type: number
                  ipAccess:
                    description: List of IP addresses allowed to
                      access the service. (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            Description of the IP address.
                          type: string
                        source:
                          description: |-
                            IP address allowed to access the service. In case you want to set the ip_access to anywhere you should set source to 0.0.0.0/0
                          type: string
                      type: object
                    type: array
                  isPrimary:
                    description: |-
                      If true, it indicates this is a primary service using its own data. If false it means this service is a secondary service, thus using data from a warehouse.
                    type: boolean
                  maxReplicaMemoryGb:
                    description: |-
                      Maximum memory of a single replica during auto-scaling in GiB.
                    type: number
                  maxTotalMemoryGb:
                    description: |-
                      Maximum total memory of all workers during auto-scaling in GiB.
                    type: number
                  minReplicaMemoryGb:
                    description: |-
                      Minimum memory of a single replica during auto-scaling in GiB.
                    type: number
                  minTotalMemoryGb:
                    description: |-
                      Minimum total memory of all workers during auto-scaling in GiB.
                    type: number
                  name:
                    description: |-
                      User defined identifier for the service.
                    type: string
                  numReplicas:
                    description: |-
                      Number of replicas for the service.
                    type: number
                  passwordWoVersion:
                    description: |-
                      Version number for password_wo. Increment this to trigger a password update when using password_wo.
                    type: number
                  privateEndpointConfig:
                    description: Service config for private endpoints
                      (see below for nested schema)
                    properties:
                      endpointServiceId:
                        description: |-
                          Unique identifier of the interface endpoint you created in your VPC with the AWS(Service Name) or GCP(Target Service) resource.
                        type: string
                      privateDnsHostname:
                        description: |-
                          Private DNS Hostname of the VPC you created.
                        type: string
                    type: object
                  queryApiEndpoints:
                    description: Configuration of the query API endpoints
                      feature. (see below for nested schema)
                    properties:
                      allowedOrigins:
                        description: |-
                          Comma separated list of domain names to be allowed cross-origin resource sharing (CORS) access to the query API. Leave this field empty to restrict access to backend servers only
                        type: string
                      apiKeyIds:
                        description: |-
                          The UUIDs of the API Keys to grant access to the query API.
                        items:
                          type: string
                        type: array
                      roles:
                        description: |-
                          The Database role that will be used to run the query.
                        items:
                          type: string
                        type: array
                    type: object
                  readonly:
                    description: |-
                      Indicates if this service should be read only. Only allowed for secondary services, those which share data with another service (i.e. when `warehouse_id` field is set).
                    type: boolean
                  region:
                    description: |-
                      Region within the cloud provider in which the service is deployed in.
                    type: string
                  releaseChannel:
                    description: |-
                      Release channel to use for this service. Can be 'default', 'fast' or 'slow'.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags associated with the service as key-value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  tier:
                    description: |-
                      Tier of the service: 'development', 'production'. Required for organizations using the Legacy ClickHouse Cloud Tiers, must be omitted for organizations using the new ClickHouse Cloud Tiers.
                    type: string
                  transparentDataEncryption:
                    description: Configuration of the Transparent Data
                      Encryption (TDE) feature. Requires an organization with the
                      Enterprise plan. (see below for nested schema)
                    properties:
                      enabled:
                        description: |-
                          If true, TDE is enabled for the service.
                        type: boolean
                      roleId:
                        description: |-
                          ID of Role to be used for granting access to the Encryption Key. This is an ARN for AWS services and a Service Account Identifier for GCP.
                        type: string
                    type: object
                  warehouseId:
                    description: |-
                      Set it to the 'warehouse_id' attribute of another service to share the data with it. The service must be in the same cloud and region.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
