---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: reverseprivateendpoints.clickpipes.clickhouse.m.crossplane.io
spec:
  group: clickpipes.clickhouse.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - clickhouse
    kind: ReversePrivateEndpoint
    listKind: ReversePrivateEndpointList
    plural: reverseprivateendpoints
    singular: reverseprivateendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ReversePrivateEndpoint is the Schema for the ReversePrivateEndpoints
          API. You can use the clickhouse_clickpipes_reverse_private_endpoint resource
          to create and manage reverse private endpoints for secure ClickPipes data
          source connections in ClickHouse Cloud. Supported endpoint types: VPC_ENDPOINT_SERVICE,
          VPC_RESOURCE, MSK_MULTI_VPC, and GCP_PSC_SERVICE_ATTACHMENT. ~> Note: All
          fields on this resource are immutable after creation. Any change will force
          replacement (destroy and recreate). Use clickhouse_clickpipes_reverse_private_endpoint_custom_private_dns
          to manage custom private DNS mappings for a reverse private endpoint.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReversePrivateEndpointSpec defines the desired state of ReversePrivateEndpoint
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      Description of the reverse private endpoint
                    type: string
                  gcpServiceAttachment:
                    description: |-
                      GCP PSC service attachment URI, required for GCP_PSC_SERVICE_ATTACHMENT type. Format: projects/{project}/regions/{region}/serviceAttachments/{name}
                    type: string
                  mskAuthentication:
                    description: |-
                      MSK cluster authentication type (SASL_IAM or SASL_SCRAM), required for MSK_MULTI_VPC type
                    type: string
                  mskClusterArn:
                    description: |-
                      MSK cluster ARN, required for MSK_MULTI_VPC type
                    type: string
                  serviceId:
                    description: |-
                      The ID of the ClickHouse service to associate with this reverse private endpoint
                    type: string
                  type:
                    description: |-
                      Type of the reverse private endpoint (VPC_ENDPOINT_SERVICE, VPC_RESOURCE, MSK_MULTI_VPC, or GCP_PSC_SERVICE_ATTACHMENT)
                    type: string
                  vpcEndpointServiceName:
                    description: |-
                      VPC endpoint service name, required for VPC_ENDPOINT_SERVICE type
                    type: string
                  vpcResourceConfigurationId:
                    description: |-
                      VPC resource configuration ID, required for VPC_RESOURCE type
                    type: string
                  vpcResourceShareArn:
                    description: |-
                      VPC resource share ARN, required for VPC_RESOURCE type
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      Description of the reverse private endpoint
                    type: string
                  gcpServiceAttachment:
                    description: |-
                      GCP PSC service attachment URI, required for GCP_PSC_SERVICE_ATTACHMENT type. Format: projects/{project}/regions/{region}/serviceAttachments/{name}
                    type: string
                  mskAuthentication:
                    description: |-
                      MSK cluster authentication type (SASL_IAM or SASL_SCRAM), required for MSK_MULTI_VPC type
                    type: string
                  mskClusterArn:
                    description: |-
                      MSK cluster ARN, required for MSK_MULTI_VPC type
                    type: string
                  serviceId:
                    description: |-
                      The ID of the ClickHouse service to associate with this reverse private endpoint
                    type: string
                  type:
                    description: |-
                      Type of the reverse private endpoint (VPC_ENDPOINT_SERVICE, VPC_RESOURCE, MSK_MULTI_VPC, or GCP_PSC_SERVICE_ATTACHMENT)
                    type: string
                  vpcEndpointServiceName:
                    description: |-
                      VPC endpoint service name, required for VPC_ENDPOINT_SERVICE type
                    type: string
                  vpcResourceConfigurationId:
                    description: |-
                      VPC resource configuration ID, required for VPC_RESOURCE type
                    type: string
                  vpcResourceShareArn:
                    description: |-
                      VPC resource share ARN, required for VPC_RESOURCE type
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.description is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.description)
                || (has(self.initProvider) && has(self.initProvider.description))'
            - message: spec.forProvider.serviceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceId)
                || (has(self.initProvider) && has(self.initProvider.serviceId))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ReversePrivateEndpointStatus defines the observed state of
              ReversePrivateEndpoint.
            properties:
              atProvider:
                properties:
                  description:
                    description: |-
                      Description of the reverse private endpoint
                    type: string
                  dnsNames:
                    description: |-
                      Reverse private endpoint internal DNS names
                    items:
                      type: string
                    type: array
                  endpointId:
                    description: |-
                      Reverse private endpoint endpoint ID
                    type: string
                  gcpServiceAttachment:
                    description: |-
                      GCP PSC service attachment URI, required for GCP_PSC_SERVICE_ATTACHMENT type. Format: projects/{project}/regions/{region}/serviceAttachments/{name}
                    type: string
                  id:
                    description: Unique identifier for the reverse private
                      endpoint
                    type: string
                  mskAuthentication:
                    description: |-
                      MSK cluster authentication type (SASL_IAM or SASL_SCRAM), required for MSK_MULTI_VPC type
                    type: string
                  mskClusterArn:
                    description: |-
                      MSK cluster ARN, required for MSK_MULTI_VPC type
                    type: string
                  privateDnsNames:
                    description: |-
                      Reverse private endpoint private DNS names
                    items:
                      type: string
                    type: array
                  serviceId:
                    description: |-
                      The ID of the ClickHouse service to associate with this reverse private endpoint
                    type: string
                  status:
                    description: |-
                      Status of the reverse private endpoint
                    type: string
                  type:
                    description: |-
                      Type of the reverse private endpoint (VPC_ENDPOINT_SERVICE, VPC_RESOURCE, MSK_MULTI_VPC, or GCP_PSC_SERVICE_ATTACHMENT)
                    type: string
                  vpcEndpointServiceName:
                    description: |-
                      VPC endpoint service name, required for VPC_ENDPOINT_SERVICE type
                    type: string
                  vpcResourceConfigurationId:
                    description: |-
                      VPC resource configuration ID, required for VPC_RESOURCE type
                    type: string
                  vpcResourceShareArn:
                    description: |-
                      VPC resource share ARN, required for VPC_RESOURCE type
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
